{
  "name": "buidash",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "BúiDash — a Geometry Dash–style rhythm runner in three.js with Guitar Hero beat-matched, authored beatmaps. Every spike lands on the beat.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "soundfont-player": "^0.12.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@tonejs/midi": "^2.0.28",
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
